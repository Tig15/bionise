import { View, Text } from "react-native";
import React from "react";

const CustomSheet = () => {
  return (
    <View>
      <Text>CustomSheet</Text>
    </View>
  );
};

export default CustomSheet;
